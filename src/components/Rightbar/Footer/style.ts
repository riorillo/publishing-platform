import { Box, styled } from "@mui/system";

export const StyledFooter = styled(Box)({
  display: "flex",
  flexWrap: "wrap",
  gap: "12px",
  marginTop: "8px",
  width: "100%",
  marginBottom: "40px",
});
